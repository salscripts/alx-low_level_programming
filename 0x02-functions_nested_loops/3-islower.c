#include "main.h"

/**
 * _islower - checks for lowercase character
 * @c: the ccharacter to check
 * Return: 1if c is lowercase, 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
